# Draft conditional task card: KURE CORAL with label-density smoothing

## Authorization and entry condition

- Proposed run ID: `kure-coral-lds-oof-v1-20260803-001`
- Sole candidate: `coral-lds-gaussian-s025-cap4`
- Status: `scientific_execution_authorized_pending_final_hash_binding_and_smoke`
- Conditional entry: run only if the separately named Stage3 phase-1 direct
  CORAL exact-OOF task fails its frozen common gate.
- Proposed resources if authorized: physical GPU0 smoke, then GPUs 0--3 exact
  five-fold training.

This is a new loss hypothesis, not integration recovery. The recorded
end-to-end user instruction authorizes only the frozen steward, smoke, and
exact-OOF stages in this card; it does not authorize refitting, validation
evaluation, deployment, or post-result retuning. Label-density smoothing (LDS) is supported by
Yang et al., *Delving into Deep Imbalanced Regression*, ICML 2021
([paper](https://proceedings.mlr.press/v139/yang21m.html),
[official code](https://github.com/YyzHarry/imbalanced-regression)). The
kernel scale and cap below are MAL2026-specific preregistered adaptations, not
values claimed by that paper.

### Fail-closed authorization state transition

The committed request begins as
`pending_direct_failure_and_scientific_authorization`: both authorization
booleans are false and every post-request lineage placeholder is blank. It
cannot generate fit-label artifacts or use a GPU.

After the direct phase-1 aggregate is complete and its frozen gate has failed,
an explicit user statement may authorize only data-steward preparation. A new
hash-bound config then uses `authorized_for_steward_preparation`, sets only the
steward boolean, binds the immutable committed pending request, this card and
preparer commits/hashes, the label-free inputs, and the exact failed direct
aggregate/task/config hashes. All fit-label manifest and artifact hashes remain
blank. The preparer accepts only this intermediate state and refuses both the
pending and final execution states.

After the fresh manifest and all 15 fit-only artifacts have been bound, a
separate explicit user statement may authorize scientific execution. Only then
may the config use `authorized`, set both authorization booleans, and bind the
manifest plus all 15 artifact hashes. The runner accepts only this final state.
Neither transition is implied by this draft or by a direct-gate failure.
The manifest binds the committed steward-authorization card through dedicated
`steward_task_card_sha256` and `steward_task_card_commit` fields. The final
config separately binds the later scientific-authorization version of this
card, so editing the card for the second authorization cannot silently rewrite
the steward artifact's provenance.
The intermediate config keeps `steward_authorized_config_sha256` blank to
avoid self-reference. The steward hashes that exact intermediate config into
the fit-label manifest; only the later final config fills
`steward_authorized_config_sha256` with that manifest-bound digest.

The exact user statements must replace the following sentinels in a committed
version of this card before the corresponding state can validate:

- Steward preparation user statement: `이거 쭉 이어서 골 목표 잘 수정 및 개선 진행하면서 하면 된다.` for `kure-coral-lds-oof-v1-20260803-001`
- Scientific execution user statement: `그래 goal도 다시 쭉 진행해보자. gpu 작업은 종료해놨고, 너가 끝까지 진행하면서 겨로가를 내면 된다.` for `kure-coral-lds-oof-v1-20260803-001`

The contextual authorization was recorded on 2026-08-03 (Asia/Seoul) after
the user was shown the conditional direct→LDS plan. It authorizes the
restricted fit-only data-steward stage now that the exact direct aggregate SHA
`4624146639b9b7c93a13850b8114169b49f6bec3997356219d000f4875fce94d`
has failed its frozen gate. The same displayed end-to-end plan and instruction
cover the conditional LDS smoke and full OOF stages. Steward preparation
completed with intermediate-config SHA-256
`ccba74503e399f6f5b6527659a964f6d7cf5cb1fdcf38d765eab01482e532861`
and fit-label manifest SHA-256
`c38d21fc9ec54c1d7140a03e74287f22316c1ab8e6e05217d8f89372061a7e58`.
Scientific execution remains fail-closed until the final committed config
binds that manifest and all 15 restricted artifact hashes.

Every repository-owned Python process in the steward and scientific stages
must use the installed `setproctitle` package. Long-running titles use the
`mal2026:lds:*` namespace and include stage plus fold/axis when applicable.
The stopped `...-004` scheduler and delayed `...-005` successor are checked
explicitly; neither is signalled or displaced.

## Frozen single candidate

For every outer fold and axis independently:

1. Use only the 1,600 outer-fit raw axis scores to form a target histogram on
   the exact score grid `1.00, 1.05, ..., 5.00`. Current canonical scores align
   exactly to this grid; runtime must reassert alignment.
2. Smooth counts with a unit-sum discrete Gaussian having physical
   `sigma=0.25` score point, truncated to `|delta| <= 0.50` (21 taps), using
   constant-zero boundaries. No alternative kernel, sigma, truncation, grid,
   or sweep is allowed.
3. At target `y`, let unscaled weight be
   `u(y) = 1 / max(smoothed_count(y), 1e-12)`.
4. Find the unique positive scalar `c` by deterministic bisection so that
   `w(y) = clip(c * u(y), 0.25, 4.0)` has an arithmetic mean of exactly 1 over
   the 1,600 fit examples, within tolerance `1e-6`. This formulation preserves
   both the cap and mean-one contract; post-cap renormalization is forbidden.

Because clipping can create a flat interval, “unique” above denotes the
canonical lower crossing rather than a claim that every possible empirical
density has a single algebraic root. Runtime starts with bracket `[0, 1]`,
doubles the upper endpoint until the clipped mean is at least 1, then performs
exactly 128 bisection updates. A midpoint with mean below 1 replaces the lower
endpoint; otherwise it replaces the upper endpoint. The final scale is the
upper endpoint, i.e. the deterministic approximation to
`inf {c > 0 : mean(clip(c*u, 0.25, 4.0)) >= 1}`. Failure to bracket within 64
doublings or to obtain absolute mean error at most `1e-6` aborts the run.

The 0.25 scale corresponds to one native rubric increment for organization
and expression, while the +/-0.5 window borrows only from a local ordinal
neighborhood. Inverse smoothed density is the direct LDS weighting mechanism;
the cap limits amplification of the extremely small score-1 groups. Unlike the
already negative effective-number and square-root-sampler candidates, the sole
new factor is local target-density smoothing.

## Training contract

- Backbone: pinned `nlpai-lab/KURE-v1` revision
  `d14c8a9423946e268a0c9952fecf3a7aabd73bd9`.
- Initialization: pinned AI-Hub full-backbone artifact SHA-256
  `ffdc985d56c655c03e8964927b127b24f0c5bb7fdde8d89e944941f5419cf25a`.
- Three independent axis models; fresh initialization per outer fold/axis.
- LoRA: rank 16, alpha 32, dropout 0.05, identical Stage3 target modules.
- Phase-1 schedule: 6 epochs, AdamW learning rate `5e-5`, weight decay `0.01`,
  batch 20, gradient accumulation 2, maximum length 1536, deterministic normal
  shuffle, no early stopping, no sampler change, and the same derived Stage3
  `coral-natural` phase-1 seed per outer fold/axis.
- No cRT, calibration, prior correction, ensemble, or second candidate.

For example `i`, rounded ordinal class uses clamped decimal half-up rounding.
The unreduced loss is:

```text
ordinal_i = mean over thresholds k=1..4 of
            BCEWithLogits(z_i,k, 1[y_round_i > k])
raw_i = (CORAL_expected_score_i - y_raw_i)^2
loss = mean_i w_i * (ordinal_i + 0.25 * raw_i)
```

The same fit-only weight is applied once to the complete per-example hybrid
loss. Applying different weights to its two terms, weighting twice, or using a
batch-normalized weight is forbidden. Inference is the natural CORAL PMF
expectation clamped to `[1,5]`.

## Isolation, execution, and evidence

- Fold assignment is loaded before scores. Fit label loaders may index only
  the three fit axis scores. Held inference objects contain ID, prompt, and
  essay but expose no label field.
- LDS density, weights, assertions, optimizer, and abort conditions are
  fit-only. Held predictions must be written atomically to a restricted fresh
  artifact before a separate evaluation loader may index held axis scores.
- The source `average` value is never indexed. No exposed validation path,
  row, rationale, prediction, or metric may enter the config or runtime.
- GPU0 smoke: outer-fold 0 content only, two fit examples from every rounded
  score, two optimizer steps, and a small label-free held inference batch. It
  is nonselectable and produces no held-gold metric. It must verify kernel sum,
  finite positive density, bounded mean-one weights, finite nonzero LoRA/head
  gradients, finite PMF/predictions, ACLs, and exact code/config hashes.
- Full: folds 0--3 start once on GPUs 0--3; fold 4 follows on GPU0. Axes run
  serially inside a fold process. Pre-existing processes are never displaced.
- Record all commands, seeds, Git/config/data/checkpoint hashes, environment,
  physical GPU mapping, 30-second telemetry, restricted ACLs, deviations, and
  5 x 400 exact-OOF coverage in append-only evidence.

## Frozen evaluation gate and stop rule

Compare once against exact R0 OOF SHA-256
`823b0c0b2114d9442e1d4e65ff7fd310e529b82d1283c78b2466b7a0141eec04`.
Report macro/per-axis continuous RMSE, Spearman, `{1,2}` RMSE, score-5 RMSE,
equal-group RMSE, gold-3/4 balanced accuracy, QWK, 3-to-4 and 4-to-3 rates,
per-band recall/one-off/RMSE, prediction-band counts, and descriptive score-1
RMSE.

Promotion requires every frozen common condition under logical AND:

- complete, finite, exact five-fold coverage and nonzero true low/high support;
- macro RMSE improvement at least `0.005`;
- no axis RMSE worsens by more than `0.01`;
- neither low-tail nor score-5 RMSE worsens;
- gold-3/4 balanced accuracy drop at most `0.01`;
- macro Spearman drop at most `0.005`;
- lower bound strictly greater than zero for the 95% interval of
  `R0 macro RMSE - candidate macro RMSE` from 10,000 source-ID-clustered
  resamples containing all three axes.

RMSE 0.4 is a stretch reporting target, not a relaxed promotion rule. “Run
once” means at most one complete exact-OOF scientific result. Before a complete
aggregate exists, an integration failure may resume only missing folds under
the identical committed code, config, data hashes, seed, and protocol; any
authenticated completed fold remains immutable and every partial or negative
artifact is preserved. If the existing artifacts cannot be resumed without an
overwrite, a separately recorded integration-recovery amendment and linked run
ID are required, with no scientific parameter change. After a complete OOF
aggregate exists, rerun, hyperparameter adjustment, or post-result calibration
is forbidden. Any gate failure permanently freezes the negative candidate,
retains exact R0, and forbids validation evaluation or refit. A GroupDRO
candidate would be a separate future scientific decision. A gate pass still
does not authorize deployment; full-train refit and a one-time descriptive
validation audit require an explicitly authorized conditional branch and may
not tune, select, or rank anything.

Every full missing-fold continuation has one immutable attempt tag. By default
it is UTC timestamp plus launcher PID and shell randomness; tests/operators may
set `MAL2026_ATTEMPT_TAG` only to a value matching
`^[A-Za-z0-9][A-Za-z0-9._-]{0,63}$`. Full telemetry, telemetry summary, worker
logs, and aggregate log are all tag-specific and no-clobber. Previous failed
attempt telemetry/logs remain preserved and do not block a new identical-hash
missing-fold attempt. A collision or dangling symlink at any path selected for
the new tag aborts before locks, GPU queries, telemetry, or worker launch. Each
ledger event records the exact attempt tag and its telemetry and summary paths.
Smoke remains a fixed-name, one-shot nonselectable gate.
