# Tested project-local runtime lock (Python 3.12, CUDA 13.0) on 2026-07-17.
# Install only into ignored .venv-standard; do not modify shared environments.
# `pip check` passed with these installed core versions before this lock was written.
torch==2.11.0
transformers==5.14.1
accelerate==1.14.0
datasets==5.0.0
peft==0.19.1
trl==0.29.1
vllm==0.25.1
# Required by FlashInfer first-use JIT; installed only in .venv-standard.
ninja==1.13.0
# Required for Trainer's aggregate-only WandbCallback; install/login remains local.
wandb==0.28.0
safetensors==0.5.3
