# Local-only model bundle

The contents of this directory, other than this file, are ignored by Git.
Generate the bundle from checksum-bound local artifacts.  Never copy training
or validation rows, generated row outputs, identifiers, predictions, logs,
optimizer states, or credentials here.
