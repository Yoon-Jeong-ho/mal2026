# Local-only R0 ensemble model bundle

This ignored directory is populated by
`scripts/prepare_r0_ensemble_submission_bundle.py`.  It contains only pinned
model snapshots, adapters, regression heads, and aggregate provenance.  Never
place evaluation rows, generated row outputs, identifiers, predictions, logs,
or credentials here.
